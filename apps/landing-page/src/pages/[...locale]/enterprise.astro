---
export const prerender = true;

import {
   ArrowRight,
   Building2,
   Calendar,
   Check,
   ChevronDown,
   ChevronRight,
   FileText,
   Github,
   Headphones,
   Mail,
   MessageSquare,
   Server,
   Settings,
   Shield,
   Users,
} from "@lucide/astro";
import type { SupportedLng } from "@packages/localization";
import { Button } from "@packages/ui/components/button";
import { supportedLanguages } from "@/i18n/config";
import StickyNavbar from "../../components/StickyNavbar.astro";
import Layout from "../../layouts/Landing.astro";
import Footer from "../../sections/Footer.astro";

export function getStaticPaths() {
   return supportedLanguages.map((locale) => ({
      params: { locale: locale === "pt-BR" ? undefined : locale },
   }));
}

const { locale } = Astro.params;
const lang = (locale || "pt-BR") as SupportedLng;

const heroStats = [
   { label: "SLA de Uptime", value: "99,9%" },
   { label: "Suporte Prioritário", value: "24/7" },
   { label: "Em Progresso", value: "SOC 2" },
   { label: "Usuários e Workspaces", value: "Ilimitados" },
];

const enterpriseFeatures = [
   {
      description:
         "Escale sua equipe sem limitações por assento. Adicione departamentos, subsidiárias e equipes de projeto livremente.",
      icon: "Users",
      title: "Usuários e Workspaces Ilimitados",
   },
   {
      description:
         "SSO/SAML, integração com Active Directory, whitelist de IP e políticas de retenção de dados personalizadas.",
      icon: "Shield",
      title: "Segurança Avançada",
   },
   {
      description:
         "Hospedagem dedicada opcional para máximo desempenho e requisitos de conformidade.",
      icon: "Server",
      title: "Infraestrutura Dedicada",
   },
   {
      description:
         "Gerente de conta dedicado, suporte 24/7, onboarding personalizado e revisões de negócio trimestrais.",
      icon: "Headphones",
      title: "Suporte Prioritário",
   },
   {
      description:
         "Cobrança flexível, DPAs personalizados, SLAs sob medida e acordos amigáveis para procurement.",
      icon: "FileText",
      title: "Contratos Personalizados",
   },
   {
      description:
         "Opções white-label, integrações personalizadas, acesso à API e automação de workflow.",
      icon: "Settings",
      title: "Personalização Avançada",
   },
];

const securityCurrentPractices = [
   "Criptografia ponta a ponta (TLS 1.3+)",
   "Criptografia em repouso (AES-256)",
   "Auditorias de segurança regulares (trimestrais)",
   "Programa de divulgação responsável (bug bounty)",
   "Autenticação multi-fator (2FA/MFA)",
   "Controle de acesso baseado em função (RBAC)",
   "Log de auditoria (retenção de 6 meses)",
   "Testes de penetração regulares",
   "Código open source (qualquer um pode auditar)",
];

const complianceRoadmap = [
   {
      items: [
         "Conformidade GDPR (implementação completa)",
         "Conformidade CCPA (privacidade EUA)",
         "Auditoria SOC 2 Tipo II (iniciada)",
      ],
      quarter: "T2 2025",
   },
   {
      items: [
         "Certificação ISO 27001 (segurança da informação)",
         "Conclusão SOC 2 Tipo II (conclusão esperada)",
      ],
      quarter: "T3 2025",
   },
   {
      items: [
         "Arquitetura pronta para HIPAA (se necessário por clientes enterprise)",
      ],
      quarter: "S2 2025",
   },
];

const securityTiers = [
   {
      features: [
         "Criptografia TLS (em trânsito)",
         "Criptografia AES-256 (em repouso)",
         "Segurança de senha (hashing, salting)",
         "2FA disponível (opcional)",
      ],
      tier: "Grátis e Pro",
   },
   {
      features: [
         "Todos os recursos Grátis/Pro",
         "2FA avançado (SMS, TOTP, chaves de hardware)",
         "Whitelist de IP",
         "Gestão de sessões",
         "Logs de auditoria (retenção completa)",
         "Controle de acesso baseado em função",
      ],
      tier: "Equipe",
   },
   {
      features: [
         "Todos os recursos Equipe",
         "Autenticação SSO / SAML",
         "Integração com Active Directory",
         "Políticas de retenção de dados personalizadas",
         "Revisão de segurança dedicada",
         "DPA personalizado (Acordo de Processamento de Dados)",
         "Conformidade com seus requisitos",
      ],
      tier: "Enterprise",
   },
];

const enterpriseSupport = [
   "Gerente de conta dedicado",
   "Contato de conformidade (para certificações)",
   "Revisão de segurança personalizada (antes do go-live)",
   "Check-ins regulares de conformidade",
   "Suporte para seus requisitos de auditoria",
];

const integrations = [
   {
      items: [
         "Okta",
         "Azure AD",
         "Google Workspace",
         "OneLogin",
         "SAML Personalizado",
      ],
      name: "Provedores SSO",
   },
   {
      items: ["QuickBooks", "Xero", "NetSuite", "SAP", "ERP Personalizado"],
      name: "Contabilidade",
   },
   {
      items: ["Slack", "Microsoft Teams", "Notion", "Webhooks", "Zapier"],
      name: "Produtividade",
   },
   {
      items: [
         "API REST",
         "GraphQL",
         "Exportação em Massa",
         "Relatórios Personalizados",
         "Data Warehouse",
      ],
      name: "Dados",
   },
];

const pricingFeatures = [
   { feature: "Usuários", value: "Ilimitados" },
   { feature: "Workspaces", value: "Ilimitados" },
   { feature: "Transações", value: "Ilimitadas" },
   { feature: "Retenção de dados", value: "Personalizada" },
   { feature: "Acesso à API", value: "Total" },
   { feature: "SSO/SAML", value: "Incluído" },
   { feature: "Suporte dedicado", value: "24/7" },
   { feature: "SLA", value: "99,9%" },
   { feature: "Integrações personalizadas", value: "Disponível" },
   { feature: "Opção on-premise", value: "Disponível" },
];

const faqs = [
   {
      answer:
         "Contratos Enterprise começam em 12 meses com opções flexíveis de renovação. Também oferecemos cobrança trimestral para implantações maiores.",
      question: "Qual é o prazo mínimo de contrato?",
   },
   {
      answer:
         "Sim. Clientes Enterprise podem escolher entre nossa nuvem gerenciada, nuvem dedicada ou implantação on-premise. Todas as opções incluem todos os recursos enterprise.",
      question: "Podemos auto-hospedar a versão enterprise?",
   },
   {
      answer:
         "O preço Enterprise é baseado nas necessidades da sua organização - número de workspaces, nível de suporte e tipo de implantação. Entre em contato conosco para uma cotação personalizada de acordo com seus requisitos.",
      question: "Como funciona o preço?",
   },
   {
      answer:
         "Somos transparentes: ainda não temos certificações formais, mas estamos trabalhando ativamente em direção ao SOC 2 Tipo II e ISO 27001 (veja nosso roadmap acima). Nossas práticas de segurança são sólidas - a documentação vem depois.",
      question: "Quais certificações vocês têm?",
   },
   {
      answer:
         "Sim. Completamos questionários de segurança para clientes enterprise. Nossa equipe de conformidade trabalhará com suas equipes de segurança e procurement.",
      question: "Vocês podem assinar nosso questionário de segurança?",
   },
   {
      answer:
         "Sim. Oferecemos pilotos de 30-90 dias para clientes enterprise avaliarem o Montte com sua equipe antes de se comprometer com uma implantação completa.",
      question: "Vocês oferecem um programa piloto?",
   },
];

const testimonials = [
   {
      avatar: "https://api.dicebear.com/9.x/notionists/svg?seed=Carlos",
      name: "Carlos R.",
      quote: "Migrar para o Montte Enterprise simplificou nossas operações financeiras em 12 departamentos. Os workspaces ilimitados significam que cada equipe tem autonomia enquanto mantemos supervisão central.",
      role: "CFO, Empresa de Tecnologia",
   },
   {
      avatar: "https://api.dicebear.com/9.x/notionists/svg?seed=Michelle",
      name: "Michelle T.",
      quote: "A integração SSO foi perfeita. Nossa equipe de TI tinha preocupações sobre segurança - a natureza open-source do Montte e o roadmap transparente os convenceram.",
      role: "Diretora de TI, Serviços Financeiros",
   },
   {
      avatar: "https://api.dicebear.com/9.x/notionists/svg?seed=David",
      name: "David L.",
      quote: "Avaliamos 5 ferramentas financeiras enterprise. A transparência de preços do Montte e a ausência de taxas por assento tornaram o cálculo de ROI simples. O suporte tem sido excepcional.",
      role: "Gerente de Operações, Manufatura",
   },
];
---

<Layout lang={lang}>
   <div class="flex flex-col">
      <StickyNavbar lang={lang}/>

      <!-- Hero Section -->
      <section class="py-16 md:py-24 bg-gradient-to-b from-primary/5 to-transparent">
         <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="text-center max-w-4xl mx-auto">
               <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 border border-primary/20 mb-8">
                  <Building2 class="w-4 h-4 text-primary" />
                  <span class="text-sm font-medium text-primary">Enterprise</span>
               </div>

               <h1 class="text-3xl font-bold sm:text-4xl lg:text-5xl xl:text-6xl leading-tight">
                  Gestão Financeira{" "}
                  <span class="text-primary">Construída para Escala</span>
               </h1>

               <p class="mt-6 text-muted-foreground text-lg sm:text-xl max-w-3xl mx-auto">
                  Usuários ilimitados. Segurança avançada. Suporte dedicado.
                  Tudo o que sua organização precisa para gerenciar finanças em escala - sem preço por assento.
               </p>

               <div class="flex flex-wrap justify-center gap-4 mt-8">
                  <a href="#contact">
                     <Button size="lg" className="group relative overflow-hidden rounded-xl h-12 px-6">
                        <span>Fale com Vendas</span>
                        <ArrowRight class="size-4 transition-transform duration-300 group-hover:translate-x-1" />
                     </Button>
                  </a>
                  <a href="#security">
                     <Button variant="outline" size="lg" className="rounded-xl h-12 px-6 gap-2">
                        <Shield class="size-5" />
                        Segurança e Conformidade
                     </Button>
                  </a>
               </div>

               <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mt-12">
                  {heroStats.map((stat) => (
                     <div class="text-center">
                        <p class="text-2xl md:text-3xl font-bold text-primary">{stat.value}</p>
                        <p class="text-sm text-muted-foreground">{stat.label}</p>
                     </div>
                  ))}
               </div>
            </div>
         </div>
      </section>

      <!-- Enterprise Features -->
      <section class="py-16 md:py-24">
         <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
               <span class="text-primary text-sm font-medium uppercase tracking-wider">
                  Recursos Enterprise
               </span>
               <h2 class="mt-4 text-2xl font-semibold md:text-3xl lg:text-4xl">
                  Tudo o Que Você Precisa para Escalar
               </h2>
               <p class="mt-4 text-muted-foreground text-lg max-w-3xl mx-auto">
                  Construído para organizações que precisam de mais do que rastreamento financeiro básico.
               </p>
            </div>

            <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
               {enterpriseFeatures.map((feature) => (
                  <div class="p-6 rounded-2xl bg-muted/30 border border-border/50 hover:border-primary/30 transition-all">
                     <div class="flex items-center justify-center w-12 h-12 rounded-xl bg-primary/10 mb-4">
                        {feature.icon === "Users" && <Users class="w-6 h-6 text-primary" />}
                        {feature.icon === "Shield" && <Shield class="w-6 h-6 text-primary" />}
                        {feature.icon === "Server" && <Server class="w-6 h-6 text-primary" />}
                        {feature.icon === "Headphones" && <Headphones class="w-6 h-6 text-primary" />}
                        {feature.icon === "FileText" && <FileText class="w-6 h-6 text-primary" />}
                        {feature.icon === "Settings" && <Settings class="w-6 h-6 text-primary" />}
                     </div>
                     <h3 class="font-semibold text-foreground mb-2">{feature.title}</h3>
                     <p class="text-muted-foreground text-sm">{feature.description}</p>
                  </div>
               ))}
            </div>
         </div>
      </section>

      <!-- Security & Compliance Section -->
      <section id="security" class="py-16 md:py-24 bg-muted/30">
         <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
               <span class="text-primary text-sm font-medium uppercase tracking-wider">
                  Segurança e Roadmap
               </span>
               <h2 class="mt-4 text-2xl font-semibold md:text-3xl lg:text-4xl">
                  Segurança e Conformidade - Integradas
               </h2>
               <p class="mt-4 text-muted-foreground text-lg max-w-3xl mx-auto">
                  Levamos segurança a sério porque suas finanças dependem disso.
               </p>
            </div>

            <!-- Current Security Practices -->
            <div class="mb-12">
               <h3 class="text-xl font-semibold text-foreground mb-6 text-center">Práticas Atuais de Segurança</h3>
               <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 max-w-4xl mx-auto">
                  {securityCurrentPractices.map((practice) => (
                     <div class="flex items-start gap-3 p-4 rounded-xl bg-background border border-border/50">
                        <Check class="w-5 h-5 text-primary flex-shrink-0 mt-0.5" />
                        <span class="text-sm text-muted-foreground">{practice}</span>
                     </div>
                  ))}
               </div>
            </div>

            <!-- Compliance Roadmap -->
            <div class="mb-12">
               <h3 class="text-xl font-semibold text-foreground mb-6 text-center">Roadmap de Conformidade</h3>
               <p class="text-center text-muted-foreground mb-8 max-w-2xl mx-auto">
                  Estamos trabalhando ativamente em certificações da indústria. Aqui está nosso cronograma:
               </p>
               <div class="grid md:grid-cols-3 gap-6">
                  {complianceRoadmap.map((item, index) => (
                     <div class={`p-6 rounded-2xl bg-background border ${index === 0 ? 'border-primary/30' : 'border-border/50'}`}>
                        <div class="flex items-center gap-2 mb-4">
                           <Calendar class={`w-5 h-5 ${index === 0 ? 'text-primary' : 'text-muted-foreground'}`} />
                           <span class="font-semibold text-foreground">{item.quarter}</span>
{index === 0 && (
                               <span class="px-2 py-0.5 text-xs rounded-full bg-primary/10 text-primary">Ativo</span>
                            )}
                        </div>
                        <ul class="space-y-2">
                           {item.items.map((i) => (
                              <li class="flex items-start gap-2 text-sm text-muted-foreground">
                                 <ChevronRight class="w-4 h-4 text-primary flex-shrink-0 mt-0.5" />
                                 <span>{i}</span>
                              </li>
                           ))}
                        </ul>
                     </div>
                  ))}
               </div>
            </div>

            <!-- Transparency Note -->
            <div class="p-6 rounded-2xl bg-primary/5 border border-primary/20 max-w-3xl mx-auto mb-12">
               <h4 class="font-semibold text-foreground mb-3">Por Que Somos Transparentes Sobre Isso</h4>
               <p class="text-muted-foreground text-sm leading-relaxed">
                  Muitas empresas alegam certificações sem realmente tê-las.
                  Estamos sendo honestos: ainda não as temos, mas estamos construindo em direção a elas.
                  Você não está confiando em um checkbox. Você está confiando em nosso compromisso de conquistá-lo.
               </p>
               <p class="mt-4 text-muted-foreground text-sm leading-relaxed">
                  Clientes enterprise: trabalharemos com você em suas necessidades específicas de conformidade.
                  Temos as práticas de segurança em vigor. A documentação vem depois.
               </p>
            </div>

            <!-- Security Features by Tier -->
            <div>
               <h3 class="text-xl font-semibold text-foreground mb-6 text-center">Recursos de Segurança por Plano</h3>
               <div class="grid md:grid-cols-3 gap-6">
                  {securityTiers.map((tier, index) => (
                     <div class={`p-6 rounded-2xl border ${index === 2 ? 'bg-primary/5 border-primary/30' : 'bg-background border-border/50'}`}>
                        <h4 class={`font-semibold mb-4 ${index === 2 ? 'text-primary' : 'text-foreground'}`}>{tier.tier}</h4>
                        <ul class="space-y-2">
                           {tier.features.map((feature) => (
                              <li class="flex items-start gap-2 text-sm text-muted-foreground">
                                 <Check class={`w-4 h-4 flex-shrink-0 mt-0.5 ${index === 2 ? 'text-primary' : 'text-primary'}`} />
                                 <span>{feature}</span>
                              </li>
                           ))}
                        </ul>
                     </div>
                  ))}
               </div>
            </div>

            <!-- Enterprise Support -->
            <div class="mt-12 p-6 rounded-2xl bg-background border border-border/50">
               <h4 class="font-semibold text-foreground mb-4 text-center">Suporte Enterprise para Conformidade</h4>
               <div class="flex flex-wrap justify-center gap-4">
                  {enterpriseSupport.map((item) => (
                     <div class="flex items-center gap-2 px-4 py-2 rounded-lg bg-muted/50">
                        <Check class="w-4 h-4 text-primary" />
                        <span class="text-sm text-muted-foreground">{item}</span>
                     </div>
                  ))}
               </div>
            </div>
         </div>
      </section>

      <!-- Integrations -->
      <section class="py-16 md:py-24">
         <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
               <span class="text-primary text-sm font-medium uppercase tracking-wider">
                  Integrações
               </span>
               <h2 class="mt-4 text-2xl font-semibold md:text-3xl lg:text-4xl">
                  Funciona Com Sua Stack
               </h2>
               <p class="mt-4 text-muted-foreground text-lg max-w-3xl mx-auto">
                  Conecte o Montte às suas ferramentas e workflows existentes.
               </p>
            </div>

            <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-6">
               {integrations.map((category) => (
                  <div class="p-6 rounded-2xl bg-muted/30 border border-border/50">
                     <h3 class="font-semibold text-foreground mb-4">{category.name}</h3>
                     <ul class="space-y-2">
                        {category.items.map((item) => (
                           <li class="text-sm text-muted-foreground flex items-center gap-2">
                              <Check class="w-4 h-4 text-primary" />
                              {item}
                           </li>
                        ))}
                     </ul>
                  </div>
               ))}
            </div>
         </div>
      </section>

      <!-- Pricing Overview -->
      <section class="py-16 md:py-24 bg-muted/30">
         <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
               <span class="text-primary text-sm font-medium uppercase tracking-wider">
                  Preços
               </span>
               <h2 class="mt-4 text-2xl font-semibold md:text-3xl lg:text-4xl">
                  Preços Enterprise Simples e Transparentes
               </h2>
               <p class="mt-4 text-muted-foreground text-lg max-w-3xl mx-auto">
                  Sem taxas por assento. Sem custos surpresa. Preços personalizados baseados nas suas necessidades.
               </p>
            </div>

            <div class="max-w-2xl mx-auto">
               <div class="p-8 rounded-2xl bg-background border border-primary/30">
                  <div class="text-center mb-8">
                     <p class="text-sm text-muted-foreground uppercase tracking-wider">Enterprise</p>
                     <p class="text-4xl font-bold text-foreground mt-2">Personalizado</p>
                     <p class="text-muted-foreground mt-2">Sob medida para sua organização</p>
                  </div>

                  <div class="grid sm:grid-cols-2 gap-4 mb-8">
                     {pricingFeatures.map((item) => (
                        <div class="flex items-center justify-between p-3 rounded-lg bg-muted/30">
                           <span class="text-sm text-muted-foreground">{item.feature}</span>
                           <span class="text-sm font-medium text-foreground">{item.value}</span>
                        </div>
                     ))}
                  </div>

                  <div class="text-center">
                     <a href="#contact">
                        <Button size="lg" className="w-full sm:w-auto rounded-xl h-12 px-8">
                           Solicite uma Cotação Personalizada
                           <ArrowRight class="size-4 ml-2" />
                        </Button>
                     </a>
                  </div>
               </div>
            </div>
         </div>
      </section>

      <!-- Testimonials -->
      <section class="py-16 md:py-24">
         <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
               <h2 class="text-2xl font-semibold md:text-3xl lg:text-4xl">
                  Confiado por Organizações em Crescimento
               </h2>
            </div>

            <div class="grid md:grid-cols-3 gap-6">
               {testimonials.map((testimonial) => (
                  <div class="relative flex flex-col p-6 md:p-8 rounded-2xl bg-muted/30 border border-border/50 hover:border-primary/30 transition-all">
                     <div class="flex-1">
                        <svg class="w-8 h-8 text-primary/30 mb-4" fill="currentColor" viewBox="0 0 24 24">
                           <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z" />
                        </svg>
                        <p class="text-foreground leading-relaxed">
                           "{testimonial.quote}"
                        </p>
                     </div>

                     <div class="flex items-center gap-4 mt-6 pt-6 border-t border-border/50">
                        <img
                           src={testimonial.avatar}
                           alt={testimonial.name}
                           class="w-12 h-12 rounded-full bg-muted"
                           loading="lazy"
                        />
                        <div>
                           <p class="font-semibold text-foreground">{testimonial.name}</p>
                           <p class="text-sm text-muted-foreground">{testimonial.role}</p>
                        </div>
                     </div>
                  </div>
               ))}
            </div>
         </div>
      </section>

      <!-- FAQ Section -->
      <section class="py-16 md:py-24 bg-muted/30">
         <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
               <h2 class="text-2xl font-semibold md:text-3xl">
                  FAQ Enterprise
               </h2>
            </div>

            <div class="max-w-3xl mx-auto space-y-4">
               {faqs.map((faq) => (
                  <details class="group p-6 rounded-2xl bg-background border border-border/50 hover:border-primary/30 transition-all">
                     <summary class="flex items-center justify-between cursor-pointer list-none">
                        <h3 class="font-semibold text-foreground pr-4">{faq.question}</h3>
                        <ChevronDown class="w-5 h-5 text-muted-foreground flex-shrink-0 transition-transform group-open:rotate-180" />
                     </summary>
                     <p class="mt-4 text-muted-foreground">{faq.answer}</p>
                  </details>
               ))}
            </div>
         </div>
      </section>

      <!-- Contact Section -->
      <section id="contact" class="py-16 md:py-24">
         <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="text-center max-w-3xl mx-auto">
               <span class="text-primary text-sm font-medium uppercase tracking-wider">
                  Comece Agora
               </span>
               <h2 class="mt-4 text-2xl font-semibold md:text-3xl lg:text-4xl">
                  Vamos Conversar Sobre Suas Necessidades
               </h2>
               <p class="mt-4 text-muted-foreground text-lg">
                  Nossa equipe enterprise trabalhará com você para entender seus requisitos
                  e criar uma solução personalizada para sua organização.
               </p>

               <div class="flex flex-wrap justify-center gap-4 mt-8">
                  <a href="mailto:enterprise@montte.co">
                     <Button size="lg" className="group relative overflow-hidden rounded-xl h-12 px-6">
                        <Mail class="size-5 mr-2" />
                        <span>enterprise@montte.co</span>
                     </Button>
                  </a>
                  <a href="#" target="_blank" rel="noopener noreferrer">
                     <Button variant="outline" size="lg" className="rounded-xl h-12 px-6 gap-2">
                        <MessageSquare class="size-5" />
                        Agende uma Demo
                     </Button>
                  </a>
               </div>

               <div class="mt-12 p-6 rounded-2xl bg-muted/30 border border-border/50">
                  <h3 class="font-semibold text-foreground mb-4">O Que Acontece Depois?</h3>
                  <div class="grid sm:grid-cols-3 gap-4 text-left">
                     <div class="flex items-start gap-3">
                        <div class="flex items-center justify-center w-8 h-8 rounded-full bg-primary/10 text-primary font-semibold text-sm flex-shrink-0">
                           1
                        </div>
                        <div>
                           <p class="font-medium text-foreground">Chamada de Descoberta</p>
                           <p class="text-sm text-muted-foreground">Conhecemos seus requisitos</p>
                        </div>
                     </div>
                     <div class="flex items-start gap-3">
                        <div class="flex items-center justify-center w-8 h-8 rounded-full bg-primary/10 text-primary font-semibold text-sm flex-shrink-0">
                           2
                        </div>
                        <div>
                           <p class="font-medium text-foreground">Demo Personalizada</p>
                           <p class="text-sm text-muted-foreground">Veja o Montte configurado para você</p>
                        </div>
                     </div>
                     <div class="flex items-start gap-3">
                        <div class="flex items-center justify-center w-8 h-8 rounded-full bg-primary/10 text-primary font-semibold text-sm flex-shrink-0">
                           3
                        </div>
                        <div>
                           <p class="font-medium text-foreground">Programa Piloto</p>
                           <p class="text-sm text-muted-foreground">Teste de 30-90 dias com sua equipe</p>
                        </div>
                     </div>
                  </div>
               </div>
            </div>
         </div>
      </section>

      <!-- Final CTA -->
      <section class="py-16 md:py-24 bg-gradient-to-t from-primary/5 to-transparent">
         <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <div class="text-center max-w-3xl mx-auto">
               <h2 class="text-2xl font-semibold md:text-3xl lg:text-4xl">
                  Pronto para Escalar Suas Operações Financeiras?
               </h2>
               <p class="mt-4 text-muted-foreground text-lg">
                  Junte-se a organizações que confiam no Montte para sua gestão financeira.
               </p>

               <div class="flex flex-wrap justify-center gap-4 mt-8">
                  <a href="mailto:enterprise@montte.co">
                     <Button size="lg" className="group relative overflow-hidden rounded-xl h-12 px-6">
                        <span>Fale com Vendas Enterprise</span>
                        <ArrowRight class="size-4 transition-transform duration-300 group-hover:translate-x-1" />
                     </Button>
                  </a>
                  <a href="https://github.com/montte/montte" target="_blank" rel="noopener noreferrer">
                     <Button variant="outline" size="lg" className="rounded-xl h-12 px-6 gap-2">
                        <Github class="size-5" />
                        Ver no GitHub
                     </Button>
                  </a>
               </div>
            </div>
         </div>
      </section>

      <Footer lang={lang}/>
   </div>
</Layout>
