---

const _costItems = [
   { name: "Conta Azul Performance", price: "R$ 849", widthClass: "w-full" },
   {
      name: "Omie ERP (empresas maiores)",
      price: "R$ 600",
      widthClass: "w-[71%]",
   },
   {
      name: "Bling Premium + contador extra",
      price: "R$ 510",
      widthClass: "w-[60%]",
   },
] as const;
---

<section  class="relative px-8 py-18 md:py-28 flex flex-col gap-12 items-center container max-w-7xl mx-auto overflow-hidden">
   <div class="p-2 flex flex-col md:flex-row border rounded-xl max-w-4xl border-border/20 bg-card/80">
      <div class="bg-gradient-to-br from-primary/10 via-primary/5 to-transparent rounded-lg px-8 py-5 flex flex-col max-w-md">
         <div class="mb-10 grow flex flex-col justify-center">
            <h3 class="font-medium text-3xl md:text-4xl leading-tight tracking-tight">
               <span class="block bg-gradient-to-br from-primary to-primary/70 bg-clip-text text-transparent">
                  Economize até 70%.
               </span>
               <span class="block bg-gradient-to-b from-foreground to-muted-foreground bg-clip-text text-transparent">
                  Invista em crescimento.
               </span>
            </h3>
            <p class="my-5 max-w-sm text-muted-foreground">
               ERPs tradicionais cobram por usuário, faturamento e módulos. Com arquitetura open-source e infraestrutura moderna, 
               o Montte elimina custos fixos abusivos — você paga apenas pelo que realmente usa.
            </p>
            <a class="-m-1 p-1 group inline-flex items-center text-primary" href="/pricing">
               <span class="mr-1">Ver preços transparentes</span>
               <ArrowRight class="transition-all relative top-px h-4 w-0 group-hover:w-4 inline" />
               <ChevronRight class="transition-all relative top-px h-4 w-4 group-hover:w-0 inline" />
            </a>
         </div>
         <p class="pt-5 border-t text-xs leading-relaxed border-border/20 text-muted-foreground">
            * Comparação baseada em preços públicos de Conta Azul, Omie e Bling para empresas com 10-50 funcionários 
            processando 5.000+ transações/mês (valores de dezembro/2025).
         </p>
      </div>

      <div class="pt-6 md:py-6 sm:min-w-96">
         <div class="px-8 md:px-16">
            <div class="text-sm text-muted-foreground">Comparação de custo mensal</div>
            <div class="text-foreground font-medium">Gestão financeira para PME (10 usuários)</div>
         </div>

         <Separator className="my-6 bg-linear-to-r from-transparent via-border/30 to-transparent" />

         {costItems.map((item, index) => (
            <>
               <div class="px-8 md:px-16">
                  <div class="flex items-center gap-3">
                     <div class="shrink-0 size-4 rounded bg-muted flex items-center justify-center">
                        <span class="text-xs text-muted-foreground font-bold">{index + 1}</span>
                     </div>
                     <div class="text-foreground font-medium">{item.name}</div>
                  </div>
                  <div class="mt-3 rounded-full bg-muted/30 p-px">
                     <div class:list={["h-2 rounded-full bg-linear-to-r from-muted-foreground/60 to-muted-foreground/40", item.widthClass]} />
                  </div>
                  <div class="text-sm mt-1.5 text-muted-foreground">
                     {item.price}/mês
                  </div>
               </div>
               {index < costItems.length - 1 && (
                  <Separator className="my-6 bg-linear-to-r from-transparent via-border/30 to-transparent" />
               )}
            </>
         ))}

         <div class="mt-6 md:mx-8 px-8 py-4 bg-gradient-to-br from-primary to-primary/85 shrink-0 text-primary-foreground rounded-xl">
            <div class="flex justify-between items-center gap-5">
               <div class="flex items-center gap-2">
                  <div class="shrink-0">
                     <img src={montteLogo} alt="Montte Logo" class="size-4" />
                  </div>
                  <div class="font-medium">Montte Empresas</div>
               </div>
               <Badge variant="secondary" className="hidden sm:flex bg-primary-foreground text-primary border-primary-foreground/20">
                  Usuários ilimitados
               </Badge>
            </div>
            <div class="mt-3 rounded-full border bg-primary-foreground/30 border-primary-foreground/10 p-px">
               <div class="h-2 rounded-full w-[29%] bg-primary-foreground" />
            </div>
            <div class="text-sm mt-1.5">
               R$ 249/mês
            </div>
         </div>
      </div>
   </div>
</section>
