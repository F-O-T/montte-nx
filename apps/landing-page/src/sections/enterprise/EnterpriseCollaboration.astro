---
import { Image } from "astro:assets";
import { ArrowRight, Check, Lock, Shield } from "@lucide/astro";
import {
   Card,
   CardDescription,
   CardHeader,
   CardTitle,
} from "@packages/ui/components/card";
import { Separator } from "@packages/ui/components/separator";
import enterpriseShield from "@/assets/cards/enterprise-shield.png";

const collaborationFeatures = [
   { title: "Permissões personalizadas por membro", icon: "Lock" },
   { title: "Histórico de alterações com autor e data", icon: "Shield" },
   { title: "Convide membros ilimitados sem custo extra", icon: "Check" },
];
---

<section  class="relative px-8 py-18 md:py-28 flex flex-col gap-12 items-center container max-w-7xl mx-auto overflow-hidden">
  <div class="flex flex-col gap-6 items-center z-10">
    <p class="text-sm uppercase font-bold text-center tracking-widest text-primary">
      Membros ilimitados
    </p>

    <h2 class="inline-block font-semibold tracking-tight text-center text-3xl md:text-5xl leading-tight max-w-3xl text-foreground">
      Toda sua equipe.
      <br />
      Sem custo adicional.
    </h2>

    <p class="sm:text-xl text-center max-w-sm sm:max-w-xl text-foreground/70">
      Adicione quantos membros precisar sem pagar por usuário.
      Todos trabalhando juntos, com permissões personalizadas e controle total.
    </p>
  </div>

  <div class="w-full grid grid-cols-1 md:grid-cols-2 gap-10 items-start">
    <Card className="overflow-hidden relative pt-0 border-none"> 
      <Image
         class="object-cover object-top w-full h-full" 
         src={enterpriseShield} 
         alt="Painel de controle enterprise do Montte"
      />

      <CardHeader>
        <CardTitle className="font-semibold text-xl md:text-2xl leading-tight">
          Código aberto, seus dados seguros
        </CardTitle>
        <CardDescription className="text-base">
          Por ser open-source, você tem transparência total sobre como seus dados são tratados.
          Exporte tudo a qualquer momento. Sem lock-in, sem surpresas.
          <a class="underline text-primary hover:text-primary/80 transition-colors ml-1" href="mailto:contato@montte.co">Falar conosco</a> se tiver dúvidas.
        </CardDescription>
      </CardHeader>
    </Card>

    <!-- Text + features -->
    <div class="flex flex-col gap-8">
      <h3 class="font-bold text-2xl md:text-3xl leading-[120%] tracking-[-0.01em] text-foreground">
        Controle de acesso simplificado
      </h3>
      <p class="text-lg md:text-xl max-w-[520px] text-muted-foreground">
        Defina quem pode ver, editar ou aprovar. Cada membro com as permissões certas.
        Histórico completo de alterações para auditoria.
      </p>

      <a class="inline-flex gap-2 items-center text-lg text-primary group" href="#faq">
        <span>Ver perguntas frequentes</span>
        <ArrowRight class="size-4 transition-transform group-hover:translate-x-1" />
      </a>

      <Separator className="bg-linear-to-r from-transparent via-primary/20 to-transparent"/>

      <div class="flex flex-col gap-8 text-foreground text-lg">
        {collaborationFeatures.map((feature) => (
          <div class="flex items-start gap-4">
            <div class="size-10 rounded-lg bg-primary/10 flex items-center justify-center flex-shrink-0">
              {feature.icon === "Shield" && (
                <Shield class="size-4 text-primary" />
              )}
              {feature.icon === "Lock" && <Lock class="size-4 text-primary" />}
              {feature.icon === "Check" && <Check class="size-4 text-primary" />}
            </div>
            <span class="font-medium leading-snug">{feature.title}</span>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
