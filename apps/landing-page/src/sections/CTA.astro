---
import { ArrowRight } from "@lucide/astro";
import type { SupportedLng } from "@packages/localization";
import { Button } from "@packages/ui/components/button";

interface Props {
   lang?: SupportedLng;
}

const { lang: _lang = "pt-BR" } = Astro.props;
---

<section id="cta" class="py-12 sm:py-16 lg:py-24">
   <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="relative overflow-hidden rounded-3xl border-none bg-black py-8 shadow-md sm:py-16 lg:py-24">
         <div class="px-8 sm:px-16 lg:px-24">
            <div class="grid grid-cols-1 justify-between gap-6 lg:grid-cols-3">
<p class="order-2 max-w-sm text-lg font-medium text-white max-lg:mx-auto max-lg:text-center lg:order-1">
                   Pronto para assumir o controle das suas finanças? Junte-se a milhares de usuários que confiam no Montte para rastreamento financeiro simples, seguro e open-source.
                </p>

                <div class="order-1 text-center text-5xl sm:text-6xl lg:text-7xl font-bold text-white lg:order-2 lg:py-7">
                   <p>Comece</p>
                   <p>Agora</p>
                </div>

                <div class="order-3 self-end justify-self-center lg:order-3 lg:justify-self-end">
                   <a href="/signup">
                      <Button
                         size="lg"
                         className="rounded-full bg-primary hover:bg-primary/90 text-base h-12 px-8 group"
                      >
                         Comece Grátis Hoje
                         <ArrowRight class="size-4 transition-transform duration-300 group-hover:translate-x-1" />
                      </Button>
                   </a>
                </div>
            </div>

            <div class="absolute bottom-0 left-14 max-lg:hidden pointer-events-none">
<div class="w-80 h-48 rounded-t-xl bg-gradient-to-br from-primary/20 to-primary/5 backdrop-blur-sm border border-white/10 flex items-center justify-center">
                   <div class="text-white/40 text-sm">Prévia do Dashboard</div>
                </div>
             </div>

             <div class="absolute top-0 right-4 max-lg:hidden pointer-events-none">
                <div class="w-80 h-48 rounded-b-xl bg-gradient-to-br from-primary/20 to-primary/5 backdrop-blur-sm border border-white/10 flex items-center justify-center">
                   <div class="text-white/40 text-sm">Prévia dos Relatórios</div>
                </div>
             </div>
         </div>
      </div>
   </div>
</section>
