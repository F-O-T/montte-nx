/**
 * Currency type re-exported from schemas for backward compatibility
 *
 * The Currency type is now defined using Zod schema inference in ../schemas.ts
 */
export type { Currency } from "../schemas";
